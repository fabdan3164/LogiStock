{% extends 'base.html.twig' %}

{% block title %}Conteneur{% endblock %}

{% block body %}
    <h1>Conteneur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ conteneur.id }}</td>
            </tr>
            <tr>
                <th>CodeConteneur</th>
                <td>{{ conteneur.codeConteneur }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ conteneur.quantite }}</td>
            </tr>
            <tr>
                <th>DateReception</th>
                <td>{{ conteneur.dateReception ? conteneur.dateReception|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_conteneur_index') }}">back to list</a>

    <a href="{{ path('app_conteneur_edit', {'id': conteneur.id}) }}">edit</a>

    <a href="{{ path('app_conteneur_print', {'id': conteneur.id}) }}">print</a>


    {{ include('conteneur/_delete_form.html.twig') }}
{% endblock %}
