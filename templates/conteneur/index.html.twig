{% extends 'base.html.twig' %}

{% block title %}Conteneur index{% endblock %}

{% block body %}
    <h1>Conteneur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Adresse</th>
                <th>CodeConteneur</th>
                <th>Quantité</th>
                <th>DateReception</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for conteneur in conteneurs %}
            <tr>
                <td>{{ conteneur.idStock }}</td>
                <td>{{ conteneur.codeConteneur }}</td>
                <td>{{ conteneur.quantite }}</td>
                <td>{{ conteneur.dateReception ? conteneur.dateReception|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('app_conteneur_show', {'id': conteneur.id}) }}">show</a>
                    <a href="{{ path('app_conteneur_edit', {'id': conteneur.id}) }}">edit</a>
                    <a href="{{ path('app_conteneur_mouvement', {'id': conteneur.id}) }}">Mouvement</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_conteneur_new') }}">Create new</a>
{% endblock %}
