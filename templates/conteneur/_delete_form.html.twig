<form method="post" action="{{ path('app_conteneur_delete', {'id': conteneur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ conteneur.id) }}">
    <button class="btn">Supprimer</button>
</form>
