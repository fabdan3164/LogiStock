<!-- Fixed navbar -->
<nav class="navbar navbar-expand-lg  mt-2">
    <div class="container">
        <a class="navbar-brand" href="/">Accueil</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMain"
                aria-controls="navbarMain" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarMain">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">


                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register_log') }}">Inscription</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_reception_index') }}">Réception</a>
                    </li>
                {% endif %}


                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_commande_index') }}">Commandes</a>
                    </li>
                {% endif %}



                {% if is_granted('ROLE_LOG') %}
                    <li class="nav-item">
                        <a class="nav-link" href="">Clients</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_stock_index') }}">Entrepot</a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            Stock
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_conteneur_index') }}">État des Stock</a>
                            </li>
                            <li><a class="dropdown-item" href="{{ path('app_flux') }}">Mouvement de stock</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_produit_index') }}">Liste des Produits</a>
                            </li>
                        </ul>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button"
                           data-bs-toggle="dropdown" aria-expanded="false">
                            Operationnel
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="{{ path('app_conteneur_new') }}">Réceptionner des
                                    produits</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_conteneur_select') }}">Déplacer</a></li>
                            <li><a class="dropdown-item" href="{{ path('app_commande_select') }}">Préparer une
                                    commande</a></li>
                        </ul>
                    </li>
                {% endif %}
                <li class="nav-item">
                    {% if app.user %}
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion {{ app.user.identifiant }} </a>
                    {% else %}
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    {% endif %}
                </li>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>
